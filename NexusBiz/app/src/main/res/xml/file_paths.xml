<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio de caché interno para imágenes temporales -->
    <cache-path name="cache_images" path="." />
    
    <!-- Directorio de caché externo para imágenes temporales -->
    <external-cache-path name="external_cache_images" path="." />
    
    <!-- Directorio de archivos externos para fotos de cámara -->
    <external-files-path name="external_files_images" path="Pictures/" />
    
    <!-- Directorio de archivos internos -->
    <files-path name="internal_files_images" path="." />
</paths>

